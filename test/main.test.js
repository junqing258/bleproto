const Long = require("long");
const ByteBuffer = require("../src/protobuf/bytebuffer");

const { expect } = require('chai');
const sinon = require('sinon');

describe('BLE Protcal Tests', () => {

    // var spy = sinon.spy();
    it('decode proto file', () => {
        const proto = {
          
        };

    });


});